<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<%= render 'users/user_info', user: @user %>
<%# あなたのみではなく、任意の注目ユーザー %>

<%= render 'books/new_book', book: @new_book %>

<h2>Books(このユーザーの投稿)</h2><%# コードはbooks/indexと全く同じ %>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
      <%# 今回は要るか？？？ %>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td>
          <%# リンクは(book.user.id)にしなくていい %>
          <%= link_to user_path(book.user) do %>
            <%# userモデルで作ったメソッド get_image %>
            <%= image_tag book.user.get_image, size: "200x200"%>
          <% end %>
        </td>
        <td><%= link_to book.title, book_path(book), method: :get %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>






